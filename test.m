%% Quick Correctness Test — All 24 NNLS Solvers
% Runs a 200x100 problem through every solver and reports PASS/FAIL.
%
% Usage:
%   run('test.m')
%
% See also: examples/quick_test.m

run(fullfile(fileparts(mfilename('fullpath')), 'examples', 'quick_test.m'));
