%% Speed Benchmark — All 24 NNLS Solvers
% Runs a 2000x1000 problem through every solver and prints a timing table.
%
% Usage:
%   run('benchmark.m')
%
% See also: examples/speed_benchmark.m

run(fullfile(fileparts(mfilename('fullpath')), 'examples', 'speed_benchmark.m'));
